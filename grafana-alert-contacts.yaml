apiVersion: 1

contactPoints:
  name: custom-email
  receivers:
    - uid: email
      type: email
      settings:
        addresses: ${GRAFANA_ALERT_RECEIVERS}
        singleEmail: true
        subject: "[Monitoring] Possible server issues!"
